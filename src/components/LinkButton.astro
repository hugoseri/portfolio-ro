---
export interface Props {
  href: string;
  className?: string;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
}

const {
  href,
  className = "",
  ariaLabel,
  title,
  disabled = false,
} = Astro.props;
---

{
  disabled ? (
    <span
      class:list={["group text-skin-title underline inline-block", className]}
      title={title}
      aria-disabled={disabled}
    >
      <slot />
    </span>
  ) : (
    <a
      {href}
      target="_blank"
      rel="noopener"
      class:list={["group text-skin-title underline inline-block hover:text-skin-title", className]}
      aria-label={ariaLabel}
      title={title}
    >
      <slot />
    </a>
  )
}
